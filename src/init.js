const settings ={
    canvas: "game1",
    character1: "img.png"

}

function initMOBSO(settings) {
 var canvas = document.getElementById(settings.canvas)
 var character1 = settings.character1
}

function makeBar(text,num,char){
    //make a div/box
    //add text
    //show char
}